const initialState = [{id:1, task: "Walk a dog", completed: false}];
//inside one task, there is id, title, completed 

export const operationsReducer = (state = initialState, action) => {
    switch(action.type){
        


        default:
            return state;
    }
}